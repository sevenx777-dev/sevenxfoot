    import React from 'react';
    import App from './index'; // Importa o componente App do seu jogo (que agora está em app/(tabs)/index.tsx)

    // Este componente será o layout para a rota de índice.
    // Ele simplesmente renderiza o seu App completo.
    export default function TabLayout() {
      return <App />;
    }
    
